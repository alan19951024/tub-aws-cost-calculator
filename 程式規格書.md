# 程式規格書

**專案名稱**：TUB 雲端架構成本計算機  
**委託單位**：台灣OO銀行 (Taiwan OO Bank)  
**部署區域**：AWS ap-northeast-1（東京）  
**文件版本**：v1.0  
**日期**：2026-04-09

---

## 目錄

1. [專案概述](#1-專案概述)
2. [專案背景與目標](#2-專案背景與目標)
3. [技術架構](#3-技術架構)
4. [檔案結構](#4-檔案結構)
5. [功能規格](#5-功能規格)
6. [AWS 架構說明](#6-aws-架構說明)
7. [成本計算邏輯](#7-成本計算邏輯)
8. [UI／UX 規格](#8-uiux-規格)
9. [專案時程](#9-專案時程)

---

## 1. 專案概述

本系統為一套純前端（Pure Frontend）互動式網頁應用，供 AWS 教學課程「賦能計畫-IV」使用。以台灣OO銀行數位轉型為情境，模擬 AWS ap-northeast-1（東京）Multi-AZ 三層式架構的月費試算，讓學員透過實際操作了解雲端成本結構。

| 項目 | 說明 |
|------|------|
| 系統類型 | 純前端 Web 應用（無後端、無資料庫） |
| 執行環境 | 瀏覽器（無需伺服器） |
| 使用語言 | HTML5、CSS3、JavaScript（ES5） |
| 外部依賴 | Google Fonts（DM Sans、DM Mono） |
| 目標使用者 | AWS 雲端架構師課程學員 |

---

## 2. 專案背景與目標

### 2.1 背景說明

台灣OO銀行擁有超過 150 萬名個人與企業客戶，長期以傳統地端（On-Premises）資料中心運行核心業務系統。面對數位金融浪潮，銀行決定啟動「數位轉型計畫」，將網路應用系統遷移至 AWS 雲端，以提升系統彈性、降低維運成本，並達到金管會要求的業務連續性（BCP）規範。

### 2.2 專案目標

| 目標 | 說明 |
|------|------|
| ⚡ 高可用性 | 採用 Multi-AZ 架構，確保 99% 系統上線率，符合金管會 BCP 規範 |
| 🔒 資安合規 | 三層式網路隔離（Public / Private / Isolated Subnet），保護客戶金融資料 |
| 📈 彈性擴展 | 透過 CloudFront + ALB 因應假日、特殊活動日等流量尖峰，自動分流 |
| 💰 成本可視化 | 建立雲端成本預估，讓管理層清楚掌握 AWS 費用結構 |

---

## 3. 技術架構

### 3.1 前端技術堆疊

```
純前端 Single Page Application (SPA)
├── HTML5        — 語意化頁面結構
├── CSS3         — 樣式、動畫、RWD、Modal
└── JavaScript   — DOM 操作、即時計算、頁籤切換（ES5 原生，無框架）
```

### 3.2 字型資源

| 字型 | 用途 | 來源 |
|------|------|------|
| DM Sans（300/400/500/700） | 主要內文、標題 | Google Fonts |
| DM Mono（400/500） | 數字、代碼、標籤 | Google Fonts |

### 3.3 CSS 設計系統（Design Tokens）

```css
--bg          : #ffffff         /* 頁面背景 */
--surface2    : #f4f6fa         /* 卡片、輸入區背景 */
--accent      : #e07b00         /* 主色（橘色） */
--accent2     : #f59500         /* 次要強調色 */
--blue        : #1a6fbf         /* 連結、資訊色 */
--green       : #1a8040         /* 成功、正確色 */
--red         : #c0392b         /* 警告、錯誤色 */
--text        : #0d1117         /* 主要文字 */
--muted       : #4a5568         /* 次要文字 */
```

---

## 4. 檔案結構

```
files/
├── index.html        # 主頁面（SPA 入口，含所有 HTML 結構）
├── tub_style.css     # 全域樣式（排版、元件、動畫、Modal）
├── tub_app.js        # 互動邏輯與動態成本計算
├── 0313.png          # 三層架構圖（課程架構示意圖）
└── README.md         # 專案說明文件
```

---

## 5. 功能規格

### 5.1 頁面頁籤（Tab Navigation）

共三個頁籤，透過 `switchTab()` 函式切換，使用 CSS `fadeIn` 動畫過場：

| 頁籤 ID | 頁籤名稱 | 內容說明 |
|---------|----------|----------|
| `tab-arch` | 專案架構圖 | 情境說明、專案目標、時間軸、架構圖 |
| `tab-summary` | 架構費用估算 | 學員練習題、情境試算三種模式、費用總計 |
| `tab-howto` | 計算詳細說明 | 各服務費率說明、計算公式、AWS Pricing Calculator 操作指引 |

### 5.2 金額模糊／顯示切換

- **元件**：Header 右上角總費用卡片
- **函式**：`revealTotal()`
- **行為**：預設以 `blur(10px)` 遮蔽金額；點擊「🔎 顯示金額」按鈕後，`filter` 切換為 `none` 顯示金額，按鈕文字同步變更為「🔒 隱藏金額」

### 5.3 架構圖全螢幕 Modal

- **觸發**：點擊架構圖（`cursor: zoom-in`）
- **函式**：`openArchModal()` / `closeArchModal()`
- **行為**：Modal 覆蓋全頁（`position: fixed; z-index: 9999`），點擊圖片外部區域或右上角 ✕ 按鈕關閉；使用 `event.stopPropagation()` 防止事件冒泡

### 5.4 學員練習題答案開關

- **函式**：`toggleAnswer(id)`
- **行為**：每題獨立控制，點擊「顯示答案」按鈕後 `.quiz-answer` 加上 `.open` class 展開答案區塊，按鈕文字切換為「隱藏答案」

### 5.5 三種情境快速切換

透過 `showScenario(id, btn)` 函式，切換不同預設參數情境：

| 情境 ID | 情境名稱 | 說明 |
|---------|----------|------|
| `sc-light` | 輕量情境 | 低流量、測試環境參數 |
| `sc-normal` | 正常情境 | 一般業務日平均用量 |
| `sc-peak` | 業務高峰 | 假日、特殊活動尖峰用量 |

### 5.6 費用明細展開／收合

- **函式**：`toggleCostDetails()`
- **行為**：切換 `#cost-detail-wrap` 的 `display: none / block`，按鈕文字在「展開明細」／「隱藏表格」之間切換

### 5.7 即時費用計算

- **函式**：`recalc()`
- **觸發時機**：頁面載入時自動執行一次（`recalc()` 於 JS 末端直接呼叫）；使用者修改輸入欄位時觸發
- **輸出位置**：動態更新多處 DOM 元素（詳見第 7 節）

---

## 6. AWS 架構說明

### 6.1 三層式架構（Three-Tier Architecture）

```
Internet
    │
    ▼
┌─────────────────────────────────────────┐
│  Web Tier（Public Subnet）               │
│  Route 53 → CloudFront → ALB            │
│  EC2 t3.medium × 2（Multi-AZ）          │
└─────────────────────────────────────────┘
    │ NAT Gateway（出站流量）
    ▼
┌─────────────────────────────────────────┐
│  AP Tier（Private Subnet）              │
│  EC2 t3.medium × 2（Multi-AZ）          │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│  DB Tier（Isolated Subnet）             │
│  Aurora PostgreSQL db.r6g.large         │
│  Primary + Replica（Multi-AZ）          │
│  S3 Standard（靜態資產 / 備份）          │
└─────────────────────────────────────────┘
```

### 6.2 AWS 服務清單

| 層級 | 服務 | 規格 | 數量 |
|------|------|------|------|
| Web Tier | Route 53 | Hosted Zone + DNS Queries | 1 |
| Web Tier | CloudFront | Asia Pacific CDN | — |
| Web Tier | ALB | Application Load Balancer | 1 |
| Web Tier | NAT Gateway | 出站流量處理 | 1 |
| Web Tier | EC2 (WEB) | t3.medium · Linux · On-Demand | 2 |
| Web Tier | EBS gp3 | 50 GB / 台 | 2 |
| AP Tier | EC2 (AP) | t3.medium · Linux · On-Demand | 2 |
| AP Tier | EBS gp3 | 50 GB / 台 | 2 |
| DB Tier | Aurora PostgreSQL | db.r6g.large · Primary + Replica | 2 |
| DB Tier | S3 Standard | 靜態資產 / 備份儲存 | 1 |

---

## 7. 成本計算邏輯

### 7.1 計算輸入參數

| 輸入欄位 ID | 服務 | 單位 |
|-------------|------|------|
| `cf-gb` | CloudFront | GB（傳輸量） |
| `alb-req` | ALB | 萬次（HTTP 請求數） |
| `nat-gb` | NAT Gateway | GB（出站流量） |
| `ec2-hr` | EC2 | 小時/天（每日運行時數） |
| `ebs-gb` | EBS gp3 | GB（每台磁碟容量） |
| `aurora-gb` | Aurora PostgreSQL | GB（資料庫儲存） |
| `s3-gb` | S3 Standard | GB（物件儲存） |

### 7.2 東京（ap-northeast-1）費率與計算公式

| 服務 | 計算公式 | 費率說明 |
|------|----------|----------|
| **CloudFront** | `cfGb × $0.120` | $0.12 / GB（Asia Pacific 傳輸） |
| **ALB** | `$5.76 + albReq × $0.22` | 固定費 + LCU 費用 |
| **NAT Gateway** | `$0.045 × 720 + natGb × $0.045 + $14.22` | 每小時費 + 流量費 + 其他固定費 |
| **EC2** | `$0.0552 × ec2Hr × 30 × 2` | t3.medium On-Demand × 2 台（WEB 或 AP） |
| **EBS gp3** | `ebsGb × $0.096 × 2` | gp3 費率 × 2 台 |
| **Aurora PostgreSQL** | `$0.3195 × 720 × 2 + auroraGb × $0.10` | db.r6g.large × 2（Primary + Replica） + 儲存費 |
| **S3 Standard** | `s3Gb × $0.025` | $0.025 / GB |

### 7.3 三層費用分組

| 分組 | 包含服務 | DOM Badge ID |
|------|----------|--------------|
| Web Tier (wT) | CloudFront + ALB + NAT | `badge-w` / `sum-w` |
| AP Tier (aT) | EC2 + EBS | `badge-a` / `sum-a` |
| DB Tier (dT) | Aurora + S3 | `badge-d` / `sum-d` |
| **總計 (tot)** | wT + aT + dT | `panel-total` / `hdr-total` |

### 7.4 視覺化長條圖

以三層費用最大值為基準（`mx = Math.max(wT, aT, dT, 1)`），動態計算各層佔比：

```javascript
bar-w.style.width = (wT / mx × 100) + '%'
bar-a.style.width = (aT / mx × 100) + '%'
bar-d.style.width = (dT / mx × 100) + '%'
```

### 7.5 課程練習題參考條件（本月使用量）

| 服務 | 條件 |
|------|------|
| Route 53 | 1 Hosted Zone · 50 萬 DNS queries |
| CloudFront | 800 GB（Asia Pacific） |
| ALB | HTTP 請求 50 萬次 / 月，單次 32 KB |
| NAT Gateway | 出站流量 80 GB |
| EC2 WEB × 2 | t3.medium · 24hr / 天 × 30 天 |
| EC2 AP × 2 | t3.medium · 24hr / 天 × 30 天 |
| EBS gp3 × 4 | 50 GB / 台（WEB×2 + AP×2） |
| Aurora PostgreSQL | db.r6g.large × 2，儲存 120 GB |
| S3 Standard | 300 GB |

---

## 8. UI／UX 規格

### 8.1 版面配置

- 最大寬度：無限制（`margin: 0 auto`）
- 主要 Padding：`100px 120px 80px`（上 / 左右 / 下）
- 背景漸層：`linear-gradient(160deg, #f0f4ff 0%, #ffffff 50%, #fff8f0 100%)`
- 頂部強調線：`border-top: 3px solid var(--accent)`（橘色）

### 8.2 主要 UI 元件

| 元件 | Class / ID | 說明 |
|------|-----------|------|
| Header | `.header` | Flex 排版，左側標題 + 右側費用卡片 |
| 費用卡片 | `.total-card` | 顯示月估算費用，支援模糊遮蔽 |
| 頁籤列 | `.tabs` / `.tab-btn` | 三頁籤切換導覽 |
| 情境橫幅 | `.scenario-banner` | 專案情境說明區塊 |
| 架構圖容器 | `.arch-wrap` | 含 hover 縮放（`scale(1.05)`）效果 |
| 架構圖 Modal | `#arch-modal` | 全螢幕覆蓋，深色背景（`rgba(0,0,0,0.65)`） |
| 練習題卡片 | `.quiz-card` | 含題目、提示、答案展開區塊 |
| 情境切換 | `.sc-tab` | 三情境面板切換 |
| 費用輸入區 | `input[type]` | 數字輸入，即時觸發 `recalc()` |
| 長條圖 | `#bar-w/a/d` | CSS width 動態比例長條 |

### 8.3 動畫規格

| 動畫 | 效果 | 時長 |
|------|------|------|
| 頁籤切換 | `fadeIn`（opacity 0→1, translateY 6px→0） | 0.25s ease |
| 金額顯示 | `filter: blur(10px)` → `none` | 0.4s ease |
| Pulse 圓點 | 縮放 + 透明度閃爍 | 2s infinite |
| 架構圖 hover | `scale(1.05)` | 0.3s ease |

---

## 9. 專案時程

| 階段 | 名稱 | 內容 | 狀態 |
|------|------|------|------|
| Phase 1 | 架構設計 | 需求分析、AWS 架構規劃、成本估算 | ✅ 進行中 |
| Phase 2 | 雲端部署 | VPC 建置、EC2/Aurora 部署、CloudFront 設定 | ⏳ 待執行 |
| Phase 3 | 資料遷移 | 現有地端資料遷移至 Aurora PostgreSQL | ⏳ 待執行 |
| Phase 4 | 上線驗收 | 壓力測試、資安稽核、正式切換 | ⏳ 待執行 |

---

## 附錄：參考資源

| 資源 | 說明 |
|------|------|
| [AWS Pricing Calculator](https://calculator.aws/#/) | 官方費用估算工具，練習題使用 |
| [AWS ap-northeast-1 定價頁](https://aws.amazon.com/pricing/) | 東京區費率參考 |
| Aurora PostgreSQL 定價 | db.r6g.large：$0.3195 / hr（東京） |
| EC2 t3.medium 定價 | $0.0552 / hr On-Demand Linux（東京） |
| EBS gp3 定價 | $0.096 / GB / month（東京） |
| S3 Standard 定價 | $0.025 / GB / month（東京） |

---

*本規格書依 `index.html`、`tub_app.js`、`tub_style.css`、`README.md` 原始碼及課程情境整理而成。*
